[build]
  publish = "dist"  # Папка, в которой будет располагаться финальная сборка
  functions = "netlify/functions"  # Папка, где хранятся серверные функции

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/json-server/:splat"
  status = 200
  force = true
